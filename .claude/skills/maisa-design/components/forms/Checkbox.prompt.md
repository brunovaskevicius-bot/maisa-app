Seleção múltipla. Componente controlado — passe `checked` e `onChange`.

```jsx
<Checkbox checked={on} onChange={e => setOn(e.target.checked)} label="Emitir NF automaticamente" description="Assim que o cliente confirmar o pagamento." />
```
