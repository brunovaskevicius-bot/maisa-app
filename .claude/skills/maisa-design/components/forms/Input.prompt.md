Campo de texto. Marque o que é opcional, nunca o que é obrigatório com asterisco.

```jsx
<Input label="WhatsApp do cliente" placeholder="(11) 90000-0000" prefix={<Icon name="phone" size={18} />} hint="É por aqui que a maisa vai falar com ele." />
```
