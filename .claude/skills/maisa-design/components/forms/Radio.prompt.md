Escolha única. Use quando as opções precisam ficar visíveis lado a lado; acima de 5 opções, prefira Select.

```jsx
<Radio name="plano" checked={plano==='pro'} onChange={()=>setPlano('pro')} label="Plano Pro" description="R$ 149/mês" />
```
