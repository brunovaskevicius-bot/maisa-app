Botão só com ícone para barras de ferramenta, cabeçalhos de card e ações de linha.

```jsx
<IconButton icon={<Icon name="ellipsis-horizontal" />} label="Mais opções" />
```

`label` é obrigatório. Variantes `ghost|outline|solid|soft`; `round` para o botão de enviar do chat.
