Abas. Máximo ~5 itens; contadores só quando o número muda a decisão do usuário.

```jsx
<Tabs value={aba} onChange={setAba} items={[{value:'hoje',label:'Hoje',count:8},{value:'semana',label:'Semana'}]} />
```
